# torch and torchvision are intentionally NOT pinned here -- they must be
# installed separately with the correct build for the reviewer's machine
# (CUDA version varies by system; a hardcoded CUDA-specific wheel here would
# fail to install entirely on a mismatched or GPU-less machine). See
# README.md "Environment setup" for the exact install command to use.
#
# Verified working combination on the development machine:
#   torch==2.5.1+cu121
#   torchvision==0.20.1+cu121

colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
filelock==3.29.0
fonttools==4.63.0
fsspec==2026.4.0
Jinja2==3.1.6
kiwisolver==1.5.0
MarkupSafe==3.0.3
matplotlib==3.11.1
mpmath==1.3.0
networkx==3.6.1
numpy==2.4.6
opencv-python-headless==5.0.0.93
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
scipy==1.17.1
six==1.17.0
sympy==1.13.1
tqdm==4.70.0
typing_extensions==4.15.0
